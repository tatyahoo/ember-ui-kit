import Ember from 'ember';
import TableBody from './ui-tbody';
import layout from '../templates/components/ui-tbody--fixed';

export default TableBody.extend({
  classNames: 'ui-tbody--fixed',
  layout
});
