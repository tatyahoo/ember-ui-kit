{{#if hasBlock}}
  {{yield list}}
{{else}}
  <ul>
    {{#each list key=key as |item|}}
      <li onclick={{action "select" item}}>
        {{#if labelPath}}
          {{get item labelPath}}
        {{else}}
          {{item}}
        {{/if}}
      </li>
    {{/each}}
  </ul>
{{/if}}
