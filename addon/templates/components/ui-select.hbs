{{yield
  (hash
    isActive=isActive
    items=selectable.listNormalized
    selected=selectable.selected
    unselected=selectable.unselected
  )
  (hash
    clear=(action "clear")
    select=(action "select")
    unselect=(action "unselect")
  )
}}
