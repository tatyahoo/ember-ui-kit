{{#if isLeafHeader}}
  <style>
    .{{columnClass}} {
      width: {{columnWidth}}px;
    }
  </style>
{{/if}}
{{yield
  (hash
    h=(component "ui-table/th" table=table thead=thead th=this frozen=frozen)
  )
}}
{{unbound frozenMirrorCellNode}}
