
import { validate } from 'dummy/helpers/validate';
import { module, test } from 'qunit';

module('Unit | Helper | validate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validate([42]);
  assert.ok(result);
});

